@page
@model EnableAuthenticatorModel
@{
    ViewData["Title"] = "Configurar Autenticador (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />

<div class="manage-card mb-4">
    <h3 class="mb-3">@ViewData["Title"]</h3>

    <p>Para ativar a autenticação em duas etapas (2FA), siga as instruções abaixo:</p>

    <ol class="list">

        <li class="mb-3">
            <p>
                Instale um aplicativo autenticador (2FA), como:
            </p>

            <ul>
                <li>
                    <strong>Microsoft Authenticator</strong> para
                    <a href="https://go.microsoft.com/fwlink/?Linkid=825072" target="_blank">Android</a> /
                    <a href="https://go.microsoft.com/fwlink/?Linkid=825073" target="_blank">iOS</a>
                </li>

                <li>
                    <strong>Google Authenticator</strong> para
                    <a href="https://play.google.com/store/apps/details?id=com.google.android.apps.authenticator2" target="_blank">Android</a> /
                    <a href="https://apps.apple.com/us/app/google-authenticator/id388497605" target="_blank">iOS</a>
                </li>
            </ul>
        </li>

        <li class="mb-4">
            <p>Escaneie o QR Code ou insira manualmente a chave abaixo no aplicativo autenticador:</p>

            <div class="p-3 bg-dark rounded border border-warning mb-3">
                <kbd class="fs-5">@Model.SharedKey</kbd>
            </div>

            <p class="text-muted">Não há diferenciação entre maiúsculas/minúsculas e espaços.</p>

            <div class="alert alert-info">
                Saiba como habilitar a geração de QR Code:
                <a href="https://go.microsoft.com/fwlink/?Linkid=852423" target="_blank">clique aqui</a>.
            </div>

            <div id="qrCode" class="my-3"></div>
            <div id="qrCodeData" data-url="@Model.AuthenticatorUri"></div>
        </li>

        <li>
            <p>
                Depois de escanear o QR Code ou inserir a chave manualmente,
                seu aplicativo autenticador exibirá um código numérico.
                Insira o código abaixo para confirmar a ativação do 2FA.
            </p>

            <div class="row mt-3">
                <div class="col-md-6">
                    <form id="send-code" method="post">
                        <div class="form-floating mb-3">
                            <input asp-for="Input.Code"
                                   class="form-control"
                                   autocomplete="off"
                                   placeholder="Código de verificação" />
                            <label asp-for="Input.Code" class="form-label">Código de verificação</label>
                            <span asp-validation-for="Input.Code" class="text-danger"></span>
                        </div>

                        <button type="submit" class="w-100 btn btn-lg btn-primary">
                            Verificar Código
                        </button>

                        <div asp-validation-summary="ModelOnly" class="text-danger mt-2" role="alert"></div>
                    </form>
                </div>
            </div>
        </li>

    </ol>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
