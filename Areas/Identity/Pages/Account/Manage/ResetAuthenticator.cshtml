@page
@model ResetAuthenticatorModel
@{
    ViewData["Title"] = "Resetar chave do autenticador";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />

<div class="manage-card mb-4">

    <h3 class="mb-3">@ViewData["Title"]</h3>

    <div class="alert alert-warning" role="alert">
        <p>
            <strong>
                Ao resetar sua chave do autenticador, seu aplicativo de autenticação
                deixará de funcionar até ser configurado novamente.
            </strong>
        </p>

        <p>
            Este processo desativa o 2FA até que você confirme sua nova configuração.
            Se você não completar a reconfiguração do autenticador, poderá perder acesso à sua conta.
        </p>
    </div>

    <form id="reset-authenticator-form" method="post">
        <button id="reset-authenticator-button" class="btn btn-danger" type="submit">
            Resetar chave do autenticador
        </button>
    </form>

</div>
