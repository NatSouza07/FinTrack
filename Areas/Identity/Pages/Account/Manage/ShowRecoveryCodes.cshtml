@page
@model ShowRecoveryCodesModel
@{
    ViewData["Title"] = "Códigos de Recuperação";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />

<h3 class="mb-3">@ViewData["Title"]</h3>

<div class="alert alert-warning" role="alert">
    <p><strong>Guarde esses códigos em um local seguro.</strong></p>
    <p>
        Se você perder o acesso ao seu dispositivo e não tiver esses códigos,
        poderá perder permanentemente o acesso à sua conta.
    </p>
</div>

<div class="manage-card p-4">
    <div class="row">
        <div class="col-md-12">

            @for (var row = 0; row < Model.RecoveryCodes.Length; row += 2)
            {
                <div class="mb-2">
                    <code class="recovery-code">@Model.RecoveryCodes[row]</code>
                    <text>&nbsp;</text>
                    <code class="recovery-code">@Model.RecoveryCodes[row + 1]</code>
                </div>
            }

        </div>
    </div>
</div>
